<h2>Registrate</h2>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

    <% @estados_array = Efederativa.all %>


    <div><%= f.label "Nombre" %><br />
      <%= f.text_field :nombre, :autofocus => true%></div>

    <div><%= f.label "Apellido Paterno" %><br />
      <%= f.text_field :ap_pat%></div>

    <div><%= f.label "Apellido Materno" %><br />
     <%= f.text_field :ap_mat%></div>

    <div><%= f.label "Entidad Federativa" %><br />

    <%= f.collection_select :efederativa_id,  Efederativa.order(:entidadFederativa), :id, :entidadFederativa, include_blank:true%>
    </div>

    <div><%= f.label "Jurisdicción sanitaria" %><br />

      <%= f.grouped_collection_select :jurisdiction_id,  Efederativa.order(:entidadFederativa), :jurisdictions,
      :entidadFederativa,:id, :jurisdiction, include_blank:true%>
    </div>

    <div><%= f.label :email %><br />
  <%= f.email_field :email %></div>

  <div><%= f.label :password %><br />
  <%= f.password_field :password %></div>

  <div><%= f.label :password_confirmation %><br />
  <%= f.password_field :password_confirmation %></div>

  <div><%= f.submit "Sign up" %></div>
<% end %>

<%= render "devise/shared/links" %>
